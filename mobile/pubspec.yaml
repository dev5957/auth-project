name: mobile
description: Client Flutter du module Auth (auth-project).
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.8.0+1
  flutter_secure_storage: ^9.2.4
  flutter_riverpod: ^2.6.1
  go_router: ^14.8.1
  google_sign_in: ^6.2.2
  image_picker: ^1.2.3
  file_picker: ^11.0.3
  video_player: ^2.10.0
  just_audio: ^0.9.46
  path_provider: ^2.1.5
  open_filex: ^4.7.0
  record: ^7.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/onboarding/
